# Google Cloud Configuration
PROJECT_ID=fechen-aifatory
LOCATION=us-central1

# Veo Video Generation Model
MODEL=veo-001

# Google Cloud Storage for Videos
GCS_VIDEOS_STORAGE_URI=gs://your-bucket-name/videos/

# Optional: If using signed URLs for video access
USE_SIGNED_URL=false

# Optional: Use Cosmo for testing (set to false for production)
USE_COSMO=false

# Google Cloud Authentication
# Either set GOOGLE_APPLICATION_CREDENTIALS to point to your service account key file
# or use Application Default Credentials (ADC) when running on Google Cloud Platform
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json