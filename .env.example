# Google Cloud Configuration
PROJECT_ID=fechen-aifatory
LOCATION=us-central1

# Veo Video Generation Model
MODEL=veo-001

# Gemini Text Generation Model (for scene generation)
# Available models: gemini-1.5-pro, gemini-1.5-flash, gemini-1.0-pro
# Try gemini-1.5-pro first, fallback to gemini-1.0-pro if needed
GEMINI_MODEL=gemini-pro

# Google Cloud Storage for Videos
GCS_VIDEOS_STORAGE_URI=gs://your-bucket-name/videos/

# Optional: If using signed URLs for video access
USE_SIGNED_URL=false

# Optional: Use Cosmo for testing (set to false for production)
USE_COSMO=false

# Google Cloud Authentication
# Either set GOOGLE_APPLICATION_CREDENTIALS to point to your service account key file
# or use Application Default Credentials (ADC) when running on Google Cloud Platform
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# YouTube Data API (REQUIRED for Reference tab functionality)
# Get your API key from Google Cloud Console > APIs & Services > Credentials
# IMPORTANT: This is REQUIRED for production deployment!
# In production, this should be stored in Google Cloud Secret Manager
# See docs/PRODUCTION_SETUP.md for production configuration
YOUTUBE_API_KEY=your_youtube_api_key_here