# Google Cloud Configuration
PROJECT_ID=fechen-aifactory
LOCATION=us-central1

# Veo Video Generation Model
MODEL=veo-001

# Google Cloud Storage for Videos (optional)
# GCS_VIDEOS_STORAGE_URI=gs://your-bucket-name/videos/

# Optional: If using signed URLs for video access
USE_SIGNED_URL=false

# Optional: Use Cosmo for testing (set to false for production)
USE_COSMO=false

# Google Cloud Authentication will use Application Default Credentials (ADC)
# Make sure you have authenticated with: gcloud auth application-default login

# Explicit project configuration for AI SDK
GOOGLE_CLOUD_PROJECT=fechen-aifactory
GCLOUD_PROJECT=fechen-aifactory
# Service Account Authentication
GOOGLE_APPLICATION_CREDENTIALS=/Users/shouian99/.config/gcloud/storycraft-service-account-key.json
